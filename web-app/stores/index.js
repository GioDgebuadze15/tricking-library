import {defineStore} from "pinia";

export const useIndexStore = defineStore({
    id: 'index-store',
    state: () => ({
    }),
    getters: {
    },
    actions: {
        async fetchMessage() {
        }
    }
})
